name: Back End CI/CD Example

defaults:
  run:
    working-directory: ./backend

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: |
          docker build -t backend .
      - name: Run tests inside Docker
        run: |
          docker run --rm backend npm run test:unit
      # =========================================================
      # As linhas abaixo estão comentadas porque não iremos subir
      # a aplicação nem rodar Terraform neste teste.
      #
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Push Docker image (exemplo)
      #   run: |
      #     IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/backend:latest
      #     docker push $IMAGE_NAME

      # - name: Terraform Apply (exemplo)
      #   run: terraform apply -auto-approve -var "image_url=${IMAGE_NAME}"
      # =========================================================
